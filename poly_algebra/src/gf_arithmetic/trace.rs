use num_bigint::BigUint;
use num_traits::One;

pub fn trace(a: &BigUint, prime_poly: &BigUint, poly_size: u32) -> BigUint {
  BigUint::one()
}

pub fn htrace(a: &BigUint, prime_poly: &BigUint, poly_size: u32) -> BigUint {
  BigUint::one()
}
